<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <title>{{ title }} — FundedList Blog</title>
  <meta name="description" content="{{ description or content | excerpt }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg: #0a0a0a;
      --surface: #141414;
      --border: #2a2a2a;
      --text: #f5f5f5;
      --text-muted: #888;
      --accent: #4f46e5;
      --accent-light: #6366f1;
      --accent-glow: rgba(99, 102, 241, 0.15);
    }
    body {
      font-family: 'Inter', -apple-system, sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }
    .container { max-width: 1100px; margin: 0 auto; padding: 0 24px; }
    nav { padding: 20px 0; border-bottom: 1px solid var(--border); position: relative; }
    nav .container { display: flex; justify-content: space-between; align-items: center; }
    .logo { font-size: 1.4rem; font-weight: 800; letter-spacing: -0.5px; text-decoration: none; color: var(--text); }
    .logo span { color: var(--accent-light); }
    .nav-links { display: flex; align-items: center; gap: 16px; }
    .nav-link { color: var(--text-muted); text-decoration: none; font-size: 13px; font-weight: 500; transition: color .2s; white-space: nowrap; }
    .nav-link:hover { color: var(--text); }
    .nav-cta { background: var(--accent); color: white; padding: 10px 24px; border-radius: 8px; text-decoration: none; font-weight: 600; font-size: 0.9rem; transition: background 0.2s; }
    .nav-cta:hover { background: var(--accent-light); }

    .post { max-width: 680px; margin: 0 auto; padding: 80px 24px 100px; }
    .post-meta { margin-bottom: 32px; }
    .post-meta a { color: var(--accent-light); text-decoration: none; font-size: 0.85rem; font-weight: 500; }
    .post-meta a:hover { text-decoration: underline; }
    .post-meta .date { color: var(--text-muted); font-size: 0.85rem; margin-top: 16px; }
    .post h1 {
      font-size: clamp(1.8rem, 4vw, 2.6rem);
      font-weight: 800;
      letter-spacing: -1px;
      line-height: 1.15;
      margin-bottom: 16px;
    }
    .post-description {
      font-size: 1.15rem;
      color: var(--text-muted);
      line-height: 1.7;
      margin-bottom: 40px;
      padding-bottom: 40px;
      border-bottom: 1px solid var(--border);
    }
    {% if image %}
    .post-hero {
      width: 100%;
      border-radius: 12px;
      margin-bottom: 40px;
      border: 1px solid var(--border);
    }
    {% endif %}
    .post-content h2 {
      font-size: 1.4rem;
      font-weight: 700;
      margin-top: 48px;
      margin-bottom: 16px;
      letter-spacing: -0.3px;
    }
    .post-content h3 {
      font-size: 1.15rem;
      font-weight: 600;
      margin-top: 36px;
      margin-bottom: 12px;
    }
    .post-content p {
      color: #ccc;
      font-size: 1rem;
      line-height: 1.85;
      margin-bottom: 20px;
    }
    .post-content a { color: var(--accent-light); text-decoration: none; }
    .post-content a:hover { text-decoration: underline; }
    .post-content ul, .post-content ol {
      color: #ccc;
      font-size: 1rem;
      line-height: 1.85;
      margin-bottom: 20px;
      padding-left: 24px;
    }
    .post-content li { margin-bottom: 8px; }
    .post-content blockquote {
      border-left: 3px solid var(--accent-light);
      padding: 16px 24px;
      margin: 24px 0;
      background: var(--surface);
      border-radius: 0 8px 8px 0;
      color: #bbb;
      font-size: 1rem;
      line-height: 1.8;
    }
    .post-content img {
      max-width: 100%;
      border-radius: 8px;
      margin: 24px 0;
      border: 1px solid var(--border);
    }
    .post-content code {
      background: var(--surface);
      padding: 2px 6px;
      border-radius: 4px;
      font-size: 0.9em;
      color: var(--accent-light);
    }
    .post-content pre {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 20px;
      overflow-x: auto;
      margin: 24px 0;
    }
    .post-content pre code {
      background: none;
      padding: 0;
      font-size: 0.88rem;
      color: #ccc;
    }
    .post-content strong { color: var(--text); }

    .post-footer {
      margin-top: 64px;
      padding-top: 40px;
      border-top: 1px solid var(--border);
      text-align: center;
    }
    .post-footer p { color: var(--text-muted); font-size: 0.95rem; margin-bottom: 20px; }
    .btn-primary {
      display: inline-flex; align-items: center; gap: 8px;
      background: var(--accent); color: white; padding: 14px 32px; border-radius: 10px;
      text-decoration: none; font-weight: 700; font-size: 1rem;
      transition: all 0.2s; box-shadow: 0 0 40px rgba(99, 102, 241, 0.25);
    }
    .btn-primary:hover { background: var(--accent-light); transform: translateY(-1px); }

    footer { padding: 40px 0; border-top: 1px solid var(--border); text-align: center; font-size: 0.85rem; color: var(--text-muted); }
    footer a { color: #888; text-decoration: none; }

    @media (max-width: 640px) {
      .post { padding: 60px 24px 80px; }
    }
    .hamburger {
      display: none;
      background: none;
      border: none;
      cursor: pointer;
      padding: 8px;
      color: var(--text);
    }
    .hamburger svg { display: block; }
    .nav-right {
      display: flex;
      align-items: center;
      gap: 16px;
    }
    .mobile-menu {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: var(--surface);
      border-bottom: 1px solid var(--border);
      padding: 16px 24px;
      z-index: 100;
    }
    .mobile-menu.open { display: block; }
    .mobile-menu a {
      display: block;
      padding: 12px 0;
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.95rem;
      font-weight: 500;
      border-bottom: 1px solid var(--border);
      transition: color .2s;
    }
    .mobile-menu a:last-child { border-bottom: none; }
    .mobile-menu a:hover { color: var(--text); }
    @media (max-width: 768px) {
      .nav-links { display: none; }
      .hamburger { display: block; }
    }
  </style>
</head>
<body>

  <nav>
    <div class="container">
      <a href="/" class="logo">Funded<span>List</span></a>
      <div class="nav-right">
        <div class="nav-links">
          <a href="/blog/" class="nav-link">Blog</a>
          <a href="/weekly-report/" class="nav-link">Weekly Report</a>
          <a href="/insurance-ma/" class="nav-link">M&amp;A Intel</a>
        </div>
        <a href="/book" class="nav-cta">Book a Call</a>
        <button class="hamburger" onclick="document.querySelector('.mobile-menu').classList.toggle('open')" aria-label="Menu">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 6h16M4 12h16M4 18h16"/></svg>
        </button>
      </div>
    </div>
    <div class="mobile-menu">
      <a href="/blog/">Blog</a>
      <a href="/weekly-report/">Weekly Report</a>
      <a href="/insurance-ma/">M&amp;A Intel</a>
      <a href="/book">Book a Call</a>
    </div>
  </nav>

  <article class="post">
    <div class="post-meta">
      <a href="/blog/">← Back to Blog</a>
      <div class="date">{{ date | readableDate }}</div>
    </div>
    <h1>{{ title }}</h1>
    {% if description %}<p class="post-description">{{ description }}</p>{% endif %}
    {% if image %}<img class="post-hero" src="{{ image }}" alt="{{ title }}">{% endif %}
    <div class="post-content">
      {{ content | safe }}
    </div>
    <div class="post-footer">
      <p>Get fresh funded founder leads every Monday.</p>
      <a href="/weekly-report/" class="btn-primary">Learn about the Weekly Report →</a>
    </div>
  </article>

  <footer>
    <div class="container">
      <p>© 2026 FundedList. Built in St. Louis. · <a href="/privacy-policy">Privacy Policy</a> · <a href="/terms-of-service">Terms of Service</a></p>
    </div>
  </footer>

</body>
</html>
